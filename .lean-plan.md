# Lean Plan: Fix architectural issues

## Task
Fix 7 architectural issues in mvp-freelance-project-search

## Steps

- [x] 1. Architect (Opus) — design fix for config race condition + SSE busy-loop
- [x] 2a. Fix Python mechanical (Sonnet) — bare except, human_delay, dead root agent_a.py
- [x] 2b. Fix JS (Sonnet) — create pythonClient.js, normalizers.js, fix index.js stub
- [x] 3. Implement race condition fix (Sonnet, main context) — main.py /api/search
- [x] 4. Implement SSE fix (Sonnet, main context) — /logs/stream
- [x] 5. code-reviewer + HIGH fixes applied

## Issues to fix
1. python/agent_a.py (root) — dead code, duplicate httpx parser, delete it
2. pages/api/projects/index.js — stub always returns [], needs real endpoint or clear intent
3. main.py /api/search — mutates global config without locking (race condition)
4. lib/pythonClient.js + lib/normalizers.js — missing, imported by parse.js
5. human_delay uses time.time()%1 instead of random
6. bare except: clauses in config.py and agents/agent_a.py
7. SSE /logs/stream — tight busy-loop sleep(0.05)
