# Lean Execution Plan — Workzilla streaming + memory-leak fixes

**Task:** Real per-card SSE streaming for Workzilla (variant A) + 3 memory-leak fixes.

## Scope (files)
- `python/agents/agent_workzilla.py` — per-card extraction inside loop (variant A)
- `python/agents/agent_a.py` — bound `found_projects`
- `python/browser.py` — Chrome lifecycle / periodic restart
- `python/main.py` — propagate client-disconnect cancellation to worker thread

## Plan

| # | Step | Model | Agent | Status |
|---|------|-------|-------|--------|
| 1 | Architecture validation (ECC step 1) — confirm variant A streaming path, Chrome lifecycle strategy, cancellation mechanism, found_projects bound | Opus | architect | [x] |
| 2 | Implement all 4 changes (well-specified after step 1) | Opus | main context (Edit) | [x] |
| 3 | Code review (ECC final) — APPROVE after 2 HIGH fixes | Sonnet | code-reviewer | [x] |
| 4 | Commit + push (Render auto-deploys) | — | main context | [x] |

## Notes
- ECC pipeline mandatory: architect → implement → code-reviewer → commit on APPROVE.
- Variant A: move extraction into the card loop so yields are genuinely incremental, not a post-extraction replay of one big `_META_JS`.
- Memory leaks: (1) `found_projects.extend` grows unbounded; (2) long-lived headless Chrome RSS growth → OOM on 512MB Render; (3) daemon worker keeps scraping under `_lock` after client disconnect.
